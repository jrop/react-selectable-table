{
  "name": "react-selectable-table",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p build/ && babel src/index.js -o build/index.js && webpack",
    "build-watch": "mkdir -p build/ && babel -w src/index.js -o build/index.js",
    "build-client-watch": "webpack -w",
    "clean": "rm -rf ./build/ ./test/build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "arrify": "^1.0.1",
    "babel-cli": "^6.8.0",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "lodash": "^4.12.0",
    "react": "^15.0.2",
    "react-dom": "^15.0.2",
    "webpack": "^1.13.0"
  },
  "peerDependencies": {
    "react": "^15.0.2"
  }
}
